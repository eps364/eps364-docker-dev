services:
  portainer:
    image: ${PORTAINER_IMAGE:-portainer/portainer-ce:latest}
    container_name: ${PORTAINER_NAME:-portainer}
    ports:
      - "${PORTAINER_PORT:-9443}:9443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data
    restart: always
    environment:
      - TZ=${TZ:-UTC}
    # Senha padrão: senha123 (ou use PORTAINER_ADMIN_PASSWORD_HASH no .env)
    command: --admin-password '$$2y$$05$$KE35iXKbsfGtvidlUu16fO1Amw8ImGTSDAiGuDRqyTQDDNuCFNzAW'

volumes:
  portainer_data: